export default function DashboardPage() {
  return (
    <>
      <h1 className="page-title">Dashboard</h1>
      <p>All backend endpoints are now accessible via the UI.</p>
      <p>Use the sidebar to test everything.</p>
    </>
  );
}
